name: "Auto Renew Bot-hosting (Matrix)"

on:
  workflow_dispatch:   # 仅手动触发

jobs:
  renew:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        account:
          - { email: "user1@example.com", session_token: "fake1", discord_token: "fake1" }
          - { email: "user2@example.com", session_token: "fake2", discord_token: "fake2" }
    steps:
      - run: echo "处理账号 ${{ matrix.account.email }}"
