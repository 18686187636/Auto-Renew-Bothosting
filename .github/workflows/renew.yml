name: Auto Renew Bot-hosting (Matrix)

on:
  schedule:
    - cron: '0 1 * * *'   # 每天 UTC 1:00 (北京时间 9:00)
  workflow_dispatch:

jobs:
  renew:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        account: ${{ fromJSON(secrets.ACCOUNTS) }}
      max-parallel: 2
    env:
      EMAIL: ${{ matrix.account.email }}
      SESSION_TOKEN: ${{ matrix.account.session_token }}
      DISCORD_TOKEN: ${{ matrix.account.discord_token }}
      GH_TOKEN: ''   # 多账号下禁用更新
      TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
      TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
      # 更健壮的默认值处理
      IS_PROXY: ${{ secrets.IS_PROXY == 'true' && 'true' || 'false' }}
      PROXY_SERVER: ${{ secrets.PROXY_SERVER || 'socks5://127.0.0.1:1080' }}
      HEADLESS: 'true'

    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v6

      - name: ⚙️ 设置 Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: 🛠️ 安装系统依赖、Chrome 和 Python 包
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb x11-utils xdotool scrot fonts-noto-cjk jq wget
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list'
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable
          pip install seleniumbase==4.20.0 requests==2.31.0
          seleniumbase install chromedriver

      - name: ⚙️ 设置代理 (sing-box)
        if: env.IS_PROXY == 'true'
        env:
          NODE_LINK: ${{ secrets.NODE_LINK }}
        run: |
          if [ -z "$NODE_LINK" ]; then
            echo "ℹ️ NODE_LINK 未设置，跳过代理"
            exit 0
          fi
          bash <(wget -qO- https://main.ssss.nyc.mn/setup_proxy.sh)
          sleep 10

      - name: 🚀 运行续期脚本（账号: ${{ matrix.account.email }}）
        run: |
          echo "开始处理账号: $EMAIL"
          xvfb-run --auto-servernum --server-args="-screen 0 1280x1024x24" python3 app.py

      - name: 🧽 清理进程和临时文件
        if: always()
        run: |
          pkill -f sing-box || true
          pkill -f chromedriver || true
          pkill -f chrome || true
          pkill -f Xvfb || true
          rm -f sing-box sing-box-config.json sing-box.log
